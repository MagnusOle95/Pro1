package application.controller;

public class Controller {

/* Fjern denne---

    //Create,delete,update og get metoder.
    public static X createX(A a, B b, C c){
        X x = new X(a,b ,c);
        Storage.addX(x);
        return x;
    }

    public static void deleteX(X x){
        Storage.removeX(x);
    }

    public static void updateX(X x, A a, B b, C c){
        x.setA(a);
        x.setB(b);
        x.setC(c);
    }

    public static ArrayList<X> getX(){
        return Storage.getX();
    }

 */// fjern denne---

    //--------------------------------------------------------------------------





    // -------------------------------------------------------------------------
    /**
     * Initializes the storage with some objects.
     */

    /* Fjern denne---

    public static void initStorage() {
        //Her tilføges alle de ting, som skal pre sættes.
        X x = Controller.createX(a, b,c );




    }

     *///Fjern denne---
}
